# Difficulty and splits

Presets: `easy` / `medium` / `hard` + frozen `EVAL_SEEDS`.

- **train** — public seeds, `evaluation_mode=False`
- **dev** — `EVAL_SEEDS`, `evaluation_mode=True`
- **hidden** — organizer seed block
